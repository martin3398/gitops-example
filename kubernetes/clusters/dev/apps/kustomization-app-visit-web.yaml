apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: app-visit-web
  namespace: flux-system
spec:
  interval: 1m
  dependsOn:
    - name: app-policies
    - name: infrastructure-security
    - name: infrastructure-gateway
  sourceRef:
    kind: GitRepository
    name: dev-repo
  path: ./kubernetes/apps/base/visit-web
  prune: true
  wait: true
  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: visit-web
      namespace: flux-system
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: visit-gateway
      namespace: flux-system
    - apiVersion: external-secrets.io/v1
      kind: ExternalSecret
      name: visit-demo-db
      namespace: visit-web
  timeout: 15m
