---
- name: Disable swap immediately
  ansible.builtin.command: swapoff -a
  when: ansible_facts["swaptotal_mb"] | default(0) | int > 0
  changed_when: true

- name: Disable swap in /etc/fstab
  ansible.builtin.replace:
    path: /etc/fstab
    regexp: '^([^#\n]\S+\s+\S+\s+swap\s+\S+\s+\S+\s+\S+)'
    replace: '# \1'
